name: Domain Renewal

on:
  push:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
      - uses: actions/checkout@v2
      - name: Domain Notification 
        run: |
          echo "送信終了"
          notification.sh
          echo "送信終了"
